# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  # Use local golden image once Packer build is complete
  # For now using ubuntu/noble64 as placeholder
  config.vm.box = "ubuntu/noble64"

  config.vm.hostname = "fw01"

  config.vm.provider "hyperv" do |h|
    h.vmname = "fw01"
    h.memory = 2048
    h.cpus   = 1
    h.enable_virtualization_extensions = false
    h.linked_clone = false
  end

  # WAN interface — management access via home network
  config.vm.network "public_network", bridge: "External Virtual Switch"

  # LAN interface — attack zone (Lab-Internal isolated switch)
  config.vm.network "public_network", bridge: "Lab-Internal"

  config.vm.provision "ansible" do |ansible|
    ansible.playbook          = "../../ansible/site.yml"
    ansible.inventory_path    = "../../ansible/inventories/hosts"
    ansible.vault_password_file = "../../.vault_pass"
    ansible.become            = true
  end
end
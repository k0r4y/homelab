---
- name: Configure all hosts
  hosts: all
  become: true
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
    - name: Install common packages
      ansible.builtin.apt:
        name:
          - curl
          - vim
          - htop
          - git
        state: present

- name: Configure mgmt01
  hosts: mgmt01
  become: true
  roles:
    - common
    - monitoring
    - mgmt01_hosts

- name: Configure gateway (fw01)
  hosts: fw01
  become: true
  roles:
    - gateway

- name: Configure target (target01)
  hosts: target01
  become: true
  roles:
    - target
    - wazuh_agent

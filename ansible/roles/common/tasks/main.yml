- name: Update apt cache
  apt:
    update_cache: yes

- name: Upgrade system packages
  apt:
    upgrade: dist

- name: Install base tools
  apt:
    name:
      - git
      - curl
      - wget
      - vim
      - tree
      - jq
    state: present

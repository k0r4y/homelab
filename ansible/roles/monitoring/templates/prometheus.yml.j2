global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]

{% for host in groups['nodes'] %}
  - job_name: "{{ host }}"
    static_configs:
      - targets: ["{{ hostvars[host]['ansible_host'] }}:9100"]
{% endfor %}
